pipeline {
    agent any

    stages {
        stage('clone') {
            steps {
                echo 'Hello World'
            }
        }
        stage('build') {
            steps {
                sh 'npm install'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploying the application' 
            }
        }
    }
}
